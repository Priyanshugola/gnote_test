import { useContext ,useEffect  } from 'react';
import Notecontext from '../context/Notecontext';

export default function About() {
  // const { upd ,updstate  } = useContext(Notecontext);
  // useEffect(() => {
  //  updstate();
  // }, [])
  return (
    <>
      about section 
    </>
  );
}
